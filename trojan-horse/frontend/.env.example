# Example environment variables for local development or Render static site
# Do NOT commit secrets to version control. Set them in the Render dashboard instead.

# Vite frontend: set the backend URL so frontend can call the API on deploy
VITE_API_URL=https://your-backend-service.onrender.com
